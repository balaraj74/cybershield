import { MainLayout } from "@/components/layout/main-layout";

export default function BreachCheckLayout({ children }: { children: React.ReactNode }) {
    return <MainLayout>{children}</MainLayout>;
}
